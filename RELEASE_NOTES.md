Release notes - 更新个人学术网站

变更摘要：
- 添加 `学习经历` 与 `工作经历` 区块，填入详细时间与机构信息。
- 将 `发表论文` 区块替换为经严格去重并按年份排序的 `publications_fragment_v2.html` 内容（132 条记录），并移至学生区之后。
- 统一页面配色与头图样式，保留白色主体背景并恢复 hero 渐变头图色调。
- 更新头像为 `Zhifeng.png`，删除旧头像 `Zhifeng.jpg`。

提交信息：
- 本次本地提交 ID 开头：`ca3d9da`。

部署：
- 已将更改推送至远程 `origin/main`（https://github.com/CCNUZFW/CCNUZFW.github.io）。
